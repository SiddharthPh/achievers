{
  "name": "hr-profile-portal",
  "version": "1.0.0",
  "description": "A full-stack HR profile portal similar to iHawk or Workday",
  "scripts": {
    "setup": "./setup.sh",
    "dev": "cd frontend && npm run dev",
    "build": "cd frontend && npm install && npm run build",
    "install:all": "cd frontend && npm install",
    "vercel-build": "cd frontend && npm install && npm run build"
  },
  "keywords": ["hr", "profile", "portal", "react", "flask", "workday"],
  "author": "Your Team",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/your-org/hr-profile-portal.git"
  }
}
